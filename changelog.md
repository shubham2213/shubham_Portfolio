# Changelog

## [2026-02-28]
- [Fix] Fixed critical BootSequence.tsx StrictMode bug — removed hasRunRef that was blocking timer setup on second useEffect run, preventing onComplete() from ever firing
- [Fix] Removed early return "if (!isVisible) return null" that was causing instant unmount without CSS fade transition
- [Refactor] Replaced isVisible state with isFading state for CSS-only fade animation (opacity 1→0 over 300ms)
- [Refactor] BootSequence now never unmounts itself — parent App.tsx handles unmounting via {!bootComplete && <BootSequence>} conditional
- [Fix] Component lifecycle now StrictMode-safe: timers always set on mount, fadeTimer at 2500ms triggers CSS fade, completeTimer at 2800ms calls onComplete(), parent unmounts component after bootComplete=true

## [2026-02-28]
- [Feature] Completed Phase 2, Task 2.8 — Final Hero Integration in App.tsx
- [Refactor] Updated App.tsx with correct z-index layering (ScanlineOverlay: 9998, SpotlightCursor: 9996, CustomCursor: 9999, NavBar: 100)
- [Feature] Added bootComplete state management using useState in App.tsx
- [Feature] Implemented BootSequence lifecycle with onComplete callback pattern
- [Feature] Wrapped BootSequence with Framer Motion AnimatePresence for smooth transitions
- [Feature] Added isInteractive prop to Hero component to control interactivity after boot
- [Feature] Initialized useScrollVelocity hook in App.tsx for chromatic aberration effect
- [Feature] Properly initialized Lenis smooth scroll in useEffect with cleanup
- [Refactor] Updated BootSequence.tsx to accept onComplete prop and trigger callback after 2500ms
- [Refactor] Converted BootSequence root element from div to motion.div with exit animation
- [Refactor] Updated Hero.tsx to accept isInteractive prop for post-boot interactivity control
- [Fix] Removed unused isVisible state from BootSequence.tsx (exit animation now handled by AnimatePresence)
- [Fix] Fixed boot sequence z-index conflict by removing AnimatePresence and implementing self-managed lifecycle in BootSequence.tsx
- [Refactor] BootSequence now controls its own fade-out via CSS transition (opacity: 1→0 over 300ms at 2500ms mark)
- [Refactor] BootSequence returns null at exactly 2800ms (2500ms + 300ms fade) to fully unmount before Hero becomes visible
- [Refactor] Removed AnimatePresence import and wrapper from App.tsx (no longer needed for boot sequence coordination)
- [Fix] Zero z-index conflict — BootSequence fully unmounts before bootComplete=true, preventing Hero interaction freeze

## [2026-02-26]
- [Fix] Created missing route files (projects, views, contact, resume) to resolve TypeScript compilation errors
- [Fix] Created missing middleware files (errorHandler, rateLimiter) to resolve TypeScript compilation errors
- [Fix] Created Prisma client export in src/prisma/index.ts
- [Fix] Updated route handlers to use correct Prisma model names (Message instead of Contact, ResumeLog instead of ResumeDownload, viewsCount instead of views)
- [Feature] Created client/src/styles/globals.css with complete CSS custom property tokens (colors, glows, gradients, fonts, spacing, animation durations, easing curves), base resets, global styles, selection color, hidden scrollbar, and Tailwind v4 import
- [Feature] Created client/src/styles/animations.css with all keyframe animations (glitch, aberration, blink, flicker, pulseGlow, travelDown, typewriter)
- [Feature] Created client/src/hooks/useCursorPosition.ts hook for tracking mouse position using refs and setProperty (no React state)
- [Feature] Created client/src/components/ui/CustomCursor.tsx component with outer ring (16px, 100ms lag) and inner dot (4px, immediate), hover interactions on interactive elements, click animation, and mobile hidden
- [Feature] Mounted CustomCursor component in client/src/App.tsx
- [Feature] Created client/src/components/ui/ScanlineOverlay.tsx component with fixed position (inset: 0), pointer-events: none, z-index: 9998, scanlines layer (repeating-linear-gradient every 4px), noise layer (SVG feTurbulence at 0.035 opacity), and flicker animation (8s infinite)
- [Feature] Mounted ScanlineOverlay component in client/src/App.tsx alongside CustomCursor
- [Feature] Created client/src/components/ui/SpotlightCursor.tsx component with 600px radial gradient (rgba(0, 245, 255, 0.04) to transparent), CSS custom properties (--cursor-x, --cursor-y), direct DOM manipulation via setProperty, z-index: 9996, pointer-events: none, and disabled on mobile (max-width: 767px)
- [Feature] Mounted SpotlightCursor component in client/src/App.tsx between CustomCursor and ScanlineOverlay
- [Feature] Created client/src/lib/gsap.ts with GSAP initialization, ScrollTrigger plugin registration, and default settings (ease: 'power3.out', duration: 0.8)
- [Feature] Created client/src/lib/lenis.ts with initLenis function that initializes Lenis with exact settings (duration: 1.2, custom easing function, vertical orientation, smoothWheel: true), integrates with GSAP ticker, and sets lagSmoothing(0)
- [Feature] Integrated Lenis smooth scroll in client/src/App.tsx using useEffect with proper cleanup (lenis.destroy() on unmount)
- [Feature] Added GSAP verification test animation in client/src/App.tsx with test box (50x50px cyan), gsap.from() animation (opacity 0→1, y: 40→0), ScrollTrigger integration with debug markers, prefers-reduced-motion check, gsap.context() wrapper, and proper cleanup with ctx.revert()
- [Refactor] Removed GSAP test animation from client/src/App.tsx after successful verification (all checks passed: GSAP context created, ScrollTrigger fired, Lenis smooth scroll working)
- [Docs] Updated docs/roadmap.md to mark Phase 1, Task 1.6 (GSAP Setup Verification) as complete — Phase 1 now 67% complete
- [Feature] Added .aberration CSS class to client/src/styles/animations.css that applies aberration keyframe animation (200ms ease-out forwards)
- [Feature] Created client/src/hooks/useScrollVelocity.ts hook that tracks scroll velocity using refs, calculates pixels per frame at 60fps, adds 'aberration' class to document.body when velocity exceeds 80px/frame, removes class on animationend event, debounced at 50ms, and disabled on mobile (max-width: 767px)
- [Feature] Exported useScrollVelocity from client/src/hooks/index.ts
- [Fix] Fixed React purity error in useScrollVelocity by initializing lastTimestamp ref with 0 and setting Date.now() inside useEffect
- [Fix] Fixed TypeScript error in useScrollVelocity by using setTimeout instead of window.setTimeout for correct type inference
- [Feature] Created client/src/components/ui/NavBar.tsx with fixed top positioning (height: 64px/--space-8), background rgba(5,5,5,0.85) with backdrop-filter blur(12px), border-bottom 1px solid --color-border-dim, z-index 100, padding 0 48px (--space-6), logo 'SS' in Orbitron 700 14px cyan with 3px letter-spacing, center nav links (INITIALIZE/IDENTITY/STACK/WORK LOG/SIGNAL/CONNECT) in JetBrains Mono 11px uppercase with 0.15em letter-spacing, hover state with color change to --color-text and '// ' prefix fade-in in cyan, active section tracking with IntersectionObserver, sound toggle placeholder button '[ ♪ SOUND ]', scroll behavior using GSAP (scrolling down → translateY -100%, scrolling up → translateY 0, 300ms ease-out transition), mobile hamburger menu with fullscreen overlay, smooth scroll to sections on click, TypeScript interfaces for props and nav links
- [Feature] Mounted NavBar component in client/src/App.tsx with 6 temporary test sections (hero, about, skills, projects, experience, contact) for testing navbar scroll behavior, active section tracking, and smooth scroll navigation
- [Feature] Created client/src/hooks/useSound.ts hook with module-level Audio instances (no React state), ambient hum (loops continuously, volume: 0.15), UI blip (volume: 0.3, plays on hover), UI click (volume: 0.4, plays on button press), sound starts muted by default (user opt-in required), graceful browser autoplay policy handling with try/catch, exposes { isPlaying, toggle, playBlip, playClick }
- [Feature] Created client/src/components/ui/SoundToggle.tsx component with useSound hook integration, off state '[ ♪ SOUND ]', on state '[ ♪ SOUND • ]' (• in cyan), JetBrains Mono font, 11px text-xs, uppercase, hover neon cyan glow, no border-radius, aria-label and aria-pressed for accessibility
- [Feature] Exported useSound from client/src/hooks/index.ts
- [Feature] Updated client/src/components/ui/NavBar.tsx to use SoundToggle component instead of placeholder button for both desktop and mobile views
- [Feature] Created client/public/sounds/ directory for sound files (ambient-hum.mp3, ui-blip.mp3, ui-click.mp3)
- [Docs] Created client/public/sounds/README.md with detailed instructions for finding CC0 licensed sounds on freesound.org, including search terms for each sound type, filtering instructions, aesthetic guidelines, and testing instructions
- [Docs] Updated docs/roadmap.md to mark Phase 1 as 100% complete (all 9 tasks: CSS Foundation, Custom Cursor, Scanline + Noise Overlay, Spotlight Cursor Effect, Lenis Smooth Scroll, GSAP Setup, Scroll Velocity Chromatic Aberration, Navbar, Ambient Sound System)
- [Docs] Updated docs/roadmap.md Quick Reference table: Current Phase now shows Phase 1 Complete, Overall Progress updated to 2/12 Phases Complete
- [Docs] Updated docs/roadmap.md Progress Summary table: Phase 1 status changed from "In Progress 33%" to "Complete 100%"
- [Docs] Added Phase 1 completion summary to docs/roadmap.md changelog with detailed breakdown of all completed features and next phase preview
- [Feature] Created client/src/components/ui/BootSequence.tsx component with fixed fullscreen positioning (inset: 0, z-index: 9995), background var(--color-bg), padding 48px (--space-6), three sequential typewriter lines in JetBrains Mono 400 --text-sm --color-green ("> INITIALIZING SYSTEM...", "> LOADING PORTFOLIO_v2.0...", "> WELCOME, VISITOR_001"), each line appears 700ms after previous using CSS typewriter animation (0.8s linear), total duration 2500ms (--duration-boot), fades out with opacity transition (300ms), unmounts from DOM after fade, useRef flag prevents re-runs, cleanup of all timers in useEffect return
- [Feature] Integrated BootSequence in client/src/App.tsx with Framer Motion AnimatePresence, bootComplete state tracks 2500ms timer, BootSequence renders first with AnimatePresence mode="wait", Hero content (motion.main) fades in with opacity 0→1 (300ms ease-out) as boot sequence fades out, coordinated transition ensures smooth handoff between boot and main content
- [Fix] Fixed BootSequence StrictMode compatibility by removing setIsVisible(false) from useRef check (was causing immediate unmount on second useEffect run in development)
- [Fix] Fixed BootSequence typewriter animation by adding display: inline-block and maxWidth: max-content to boot lines (gives proper width boundary for width: 0→100% animation)
- [Fix] Added gap: var(--space-2) (16px) to BootSequence container for proper vertical spacing between lines
- [Fix] Fixed NavBar visibility during boot sequence by conditionally rendering NavBar only when bootComplete is true in App.tsx
- [Docs] Updated docs/roadmap.md to reflect Phase 2 progress: marked Phase 2.1 (Boot Sequence) as complete, updated Current Phase to "Phase 2 — Hero Section (In Progress)", updated Overall Progress to show Phase 2 at 14% completion, updated Progress Summary table to show Phase 2 status as "In Progress 14%", added detailed Phase 2.1 completion entry to changelog section with all implementation details and fixes

## [2026-02-28]
- [Feature] Added GSAP hero entry animation to client/src/components/sections/Hero.tsx with timeline that fires after boot sequence completes (2.3s delay), animates name and ParticleField simultaneously (y: 30→0, opacity: 0→1, duration: 0.8s, ease: power4.out), tagline animates 0.2s after name (y: 30→0, opacity: 0→1), NeonButton animates 0.4s after name (y: 30→0, opacity: 0→1), HUD tags animate 0.6s after name (opacity: 0→1), all elements start at opacity: 0 before animation fires, uses gsap.context() for cleanup with ctx.revert() in useEffect return, prefers-reduced-motion check skips animation and sets opacity: 1 immediately, coordinates with BootSequence fade-out for seamless transition
- [Feature] Added scroll indicator to client/src/components/sections/Hero.tsx at bottom center (bottom: var(--space-6), absolute positioning, z-index: 4), vertical line (2px wide, 40px tall, --color-text-dim), animated dot traveling down line (4px circle, --color-text-dim, travelDown keyframe 3000ms infinite), "SCROLL" text below line (Orbitron 400, 10px, letter-spacing: 4px, --color-text-dim), entire indicator centered horizontally with transform: translateX(-50%)
- [Feature] Added GSAP ScrollTrigger fade-out animation for scroll indicator in client/src/components/sections/Hero.tsx that fires when user scrolls 100px down from top (trigger: document.body, start: 'top -100px'), fades out opacity: 1→0 with duration: 0.3s ease: power2.out, toggleActions: 'play none none none' ensures it stays hidden after fade, uses gsap.context() for cleanup with ctx.revert() in useEffect return, prefers-reduced-motion check skips animation

## [2026-02-27]
- [Feature] Created client/src/components/canvas/ParticleField.tsx with React Three Fiber Canvas, 80 particles (desktop), 40 particles (tablet), 0 particles (mobile), Points in 3D space with random distribution (-10 to +10 x/y, -5 to +5 z), particle color #00f5ff at 0.6 opacity, particle size 1.5px, connection lines between particles within 150px distance, line color #00f5ff at 0.15 opacity, slow drift animation using useFrame (velocity: 0.0005 units/frame), mouse interaction (particles within 100px repel with force: 0.02, inverse square fallback), spring return to origin (force: 0.005), proper geometry and material disposal on unmount, lazy loaded with React.lazy, absolute positioning (z-index: 2), pointer-events: none
- [Feature] Created client/src/components/canvas/GridBackground.tsx with CSS-only implementation (no Three.js), thin cyan grid lines (rgba(0, 245, 255, 0.04)), 40px × 40px grid cell size, covers 120% of hero section (handles parallax overflow), mouse parallax shift (2% of cursor offset via transform translate), direct DOM manipulation using ref.style.transform, absolute positioning (z-index: 1), pointer-events: none, lazy loaded with React.lazy, disabled on mobile (max-width: 767px)
- [Feature] Added TypeScript interfaces to client/src/types/index.ts: MousePosition interface with x/y numbers, ParticleFieldProps interface with mousePosition ref, GridBackgroundProps interface with mousePosition ref
- [Fix] Fixed React Three Fiber bufferAttribute TypeScript error in ParticleField.tsx by replacing count/array/itemSize props with args={[array, itemSize]} format (correct R3F constructor syntax)
- [Fix] Fixed 8 ESLint errors in ParticleField.tsx: replaced useMemo with useState initializer functions for particleCount and particles (Math.random is impure, cannot be called during render), captured ref values in useEffect cleanup to prevent stale closure warnings
- [Feature] Updated client/src/hooks/useCursorPosition.ts to return normalized coordinates from -1 to 1 based on viewport center (x = (mouseX / windowWidth - 0.5) * 2, y = (mouseY / windowHeight - 0.5) * 2), added mobile detection (max-width: 767px) that returns {x: 0, y: 0} when mobile, added resize listener to update mobile state, uses ref.style.setProperty — NEVER React state
- [Feature] Created client/src/hooks/useParallax.ts custom hook that takes multiplier (0 to 1) and maxOffset (default: 50px), calculates offset based on normalized cursor position and multiplier, updates element transform via ref.style.transform directly using requestAnimationFrame, returns elementRef to attach to target element, disabled on mobile, example multipliers: 0.02 = background, 0.04 = particles, 0.06 = HUD, 0 = no movement
- [Refactor] Updated client/src/components/canvas/GridBackground.tsx to use useParallax hook with multiplier: 0.02 (Layer 1 — subtle background movement), removed old mouse position handling, added willChange: transform for performance, removed transition property (now handled by requestAnimationFrame)
- [Refactor] Updated client/src/components/canvas/ParticleField.tsx to use useParallax hook with multiplier: 0.04 (Layer 2 — medium particle movement), wrapped entire Canvas in parallax container div, added willChange: transform for performance
- [Feature] Created client/src/components/sections/Hero.tsx with 4-layer parallax system: Layer 1 (GridBackground, multiplier: 0.02), Layer 2 (ParticleField, multiplier: 0.04), Layer 3 (HUD elements with coordinates/status/timestamp, multiplier: 0.06), Layer 4 (Name + CTA content, multiplier: 0), uses Suspense for lazy-loaded canvas components, HUD elements in all 4 corners (LAT/LONG coordinates, SYSTEM: ONLINE status, ISO date timestamp), hero name in Orbitron 900 clamp(3rem, 8vw, 7rem) white, tagline in JetBrains Mono, CTA button 'ENTER SYSTEM_' with cyan border and glow hover, smooth scroll to projects section on click
- [Feature] Updated client/src/App.tsx to import and mount Hero component, replaced temporary hero section placeholder with new Hero component featuring multi-layer parallax
- [Fix] Created client/src/hooks/useCursorPixelPosition.ts to fix CustomCursor stuck at top-left issue — implements separation of concerns pattern (industry standard): useCursorPosition returns normalized coordinates (-1 to 1) for parallax effects, useCursorPixelPosition returns raw pixel coordinates for UI positioning, follows Single Responsibility Principle with zero coupling between concerns
- [Refactor] Updated client/src/components/ui/CustomCursor.tsx to use useCursorPixelPosition hook instead of useCursorPosition (was receiving normalized coordinates instead of pixels, causing cursor to be stuck at 0,0)
- [Refactor] Updated client/src/components/ui/SpotlightCursor.tsx to use useCursorPixelPosition hook with requestAnimationFrame loop for consistency and performance (previously used direct event listener)
- [Feature] Exported useCursorPixelPosition from client/src/hooks/index.ts for centralized hook access
- [Feature] Created client/src/components/ui/GlitchText.tsx component with props (text: string, intensity: 'low'|'medium'|'high', interval: number ms default 5000, className?: string, as?: 'h1'|'h2'|'h3'|'span'|'p' default 'span'), structure with glitch-wrapper containing glitch-main + glitch-before + glitch-after layers, intensity levels (low: ±2px offset 150ms, medium: ±4px offset 200ms, high: ±6px offset 300ms), before layer uses --color-cyan with translateX(-offset), after layer uses --color-green with translateX(+offset), glitch fires via setInterval at specified interval, cleanup setInterval on unmount, aria-hidden on before/after layers for accessibility, CSS custom properties for dynamic offset values
- [Feature] Added GlitchText component styles to client/src/styles/globals.css: .glitch-wrapper (position: relative, display: inline-block), .glitch-main (position: relative, z-index: 1), .glitch-before and .glitch-after (position: absolute, top: 0, left: 0, width: 100%, height: 100%, opacity: 0, pointer-events: none), .glitch-before.active (color: var(--color-cyan), opacity: 0.8, translateX using --glitch-offset CSS variable), .glitch-after.active (color: var(--color-green), opacity: 0.8, translateX using --glitch-offset CSS variable)
- [Fix] Fixed TypeScript export errors in client/src/components/ui/index.ts by changing from default exports to named exports for BootSequence, CustomCursor, NavBar, ScanlineOverlay, SoundToggle, and SpotlightCursor (kept GlitchText as default export since it uses export default pattern)
- [Feature] Created client/src/components/ui/NeonButton.tsx component with props extending ButtonHTMLAttributes<HTMLButtonElement>, variant prop ('cyan' | 'green' default 'cyan'), Orbitron 700 font --text-xs uppercase 0.15em letter-spacing, transparent background with 1px solid border (--color-cyan or --color-green), padding --space-2 --space-4 (16px 32px), cursor: none (custom cursor handles it), hover state (background rgba with 0.08 opacity, box-shadow --glow-cyan or --glow-green, transition --duration-base --ease-out), active/click state (transform scale(0.98), background rgba 0.15 opacity), all colors use CSS variables
- [Feature] Created client/src/components/ui/HUDTag.tsx component with props (children: ReactNode, accentColor?: 'cyan'|'green'|'dim' default 'dim', animated?: boolean default false, className?: string), JetBrains Mono 400 --text-2xs (0.625rem), letter-spacing 0.1em uppercase, opacity 0.6, color based on accentColor prop (--color-cyan, --color-green, or --color-text-dim), optional float animation (translateY ±4px, 4s ease-in-out infinite)
- [Feature] Added float keyframe animation to client/src/styles/animations.css for HUDTag animated prop (0%/100%: translateY(0), 50%: translateY(-4px))
- [Feature] Exported NeonButton and HUDTag from client/src/components/ui/index.ts for centralized component access
- [Feature] Added style prop (React.CSSProperties) to GlitchText component interface and passed through to wrapper Element for inline styling support
- [Feature] Completed client/src/components/sections/Hero.tsx with full Phase 2 Task 2.5 specifications: full viewport height (100dvh), section id="hero", content centered vertically and horizontally, 4-layer background system (GridBackground z-index 1, ParticleField z-index 2, HUD elements z-index 3, main content z-index 4), GlitchText component with "SHUBHAM SHRIVASTAVA" text (Orbitron 900, clamp(3rem, 8vw, 7rem), white, intensity: medium, interval: 5000ms, as: h1), tagline "Frontend Engineer // 2.7 Years // Currently: VERTEXC3" (JetBrains Mono 400, --color-text, '//' separators in --color-cyan), NeonButton "[ ENTER SYSTEM ]_" with blinking underscore (blink keyframe animation 1000ms infinite), onClick scrolls smoothly to #about section, HUD tags bottom-left "LAT: 20.5937° N  LNG: 78.9629° E" and bottom-right live clock (HH:MM:SS IST format, updates every second via setInterval, cleanup on unmount), floating decorative HUD elements with low opacity (0.25-0.3) and animated prop ("SYS_VER: 2.0.1", "NODE: ACTIVE" in green, "SIGNAL: ████████░░  80%"), scroll indicator at bottom with vertical line (1px --color-border, 40px height), traveling dot (4px cyan circle, travelDown animation 3000ms infinite), "SCROLL" text (Orbitron 400, 10px, --color-text-dim, 4px letter-spacing), fades out on scroll using opacity transition based on scrollY position, TypeScript interface for Hero props (no required props), all absolute positioned elements use CSS variables for spacing (--space-4, --space-6, --space-8, --space-10, --space-12, --space-16, --space-20), live clock cleanup with clearInterval on unmount, scroll event listener cleanup on unmount
